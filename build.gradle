ext {
    projectVersion = '0.1.0' + (Boolean.valueOf(System.getProperty('release')) ? '' : '-SNAPSHOT')
}

buildscript {
    repositories {
        gradlePluginPortal()
    }
}


subprojects {
    repositories {
        mavenCentral()
    }
}

wrapper {
    gradleVersion = "7.1.1"
}