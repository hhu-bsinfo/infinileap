bintray {
    user = System.getProperty('bintray.user')
    key = System.getProperty('bintray.key')
    publications = ['mavenPublication']
    publish = true
    override = true

    pkg {
        repo = 'infinileap'
        name = 'core'
        userOrg = 'hhu-bsinfo'
        licenses = ['GPL-3.0']
        vcsUrl = 'https://github.com/hhu-bsinfo/infinileap.git'
        labels = ['infiniband', 'ucx', 'java', 'panama', 'networking', 'high-performance']
        githubRepo = 'hhu-bsinfo/infinileap'
        githubReleaseNotesFile = 'README.md'

        version {
            name = project.version
            released  = new Date()
        }
    }
}